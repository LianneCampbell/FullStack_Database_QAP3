<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Books</title>
</head>
<body>
    <h1>Books</h1>
    <a href="/books/new">Add New Book</a>
    <ul>
        <% books.forEach(book => { %>
            <li>
                <%= book.title %> - <%= book.price %> - <%= book.genre %> 
                <a href="/books/<%= book.book_id %>/edit">Edit</a>
                <form action="/books/<%= book.book_id %>?_method=DELETE" method="POST">
                    <button type="submit">Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>